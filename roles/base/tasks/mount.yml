---

- name: Mount tmpfs
  mount:
    src: tmpfs
    name: "{{ item.src }}"
    fstype: tmpfs
    opts: "defaults,{{ item.options }},size={{ item.size }}"
    dump: "0"
    passno: "0"
    state: mounted
  with_items: "{{ merged.system.mounts.tmpfs }}"
  when: merged.system.mounts.tmpfs|length > 0
