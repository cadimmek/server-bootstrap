#!/bin/bash
IFS= read -r -d '' MODEL </proc/device-tree/model || [[ $MODEL ]]
PI="false"
if [[ $MODEL == *"Raspberry Pi"* ]]; then
  PI="true"
fi

cat <<EOF
{
 "name" : "$MODEL",
 "pi"   : $PI
}
EOF

